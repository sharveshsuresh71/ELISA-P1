# LiveKit project credentials — from cloud.livekit.io (Settings > Keys)
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# LiveKit server URL, e.g. wss://your-project.livekit.cloud
# Exposed to the browser (NEXT_PUBLIC_) so it can join the room directly.
NEXT_PUBLIC_LIVEKIT_URL=
